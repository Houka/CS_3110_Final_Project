open Feunit
open Terrain

let update (units:feunit array) (terrains:terrain array) : unit =
  failwith "TODO"

(* finds enemy units to move*)
let find_enemy_units (units:feunit list) : feunit list = failwith "TODO"

(* Algorithm 1: Rush nearest*)
let rush_nearest (units:feunit array) (terrains:terrain array) : unit =
  failwith "TODO"

(* Algorithm 2: Swarm Lowest Health *)
let attack_weakest (units:feunit array) (terrains:terrain array) : unit =
  failwith "TODO"

(* Algorithm 3: Focus On Easiest Unit To Take Down *)
let focus_super_effective (units:feunit array) (terrains:terrain array) : unit =
  failwith "TODO"