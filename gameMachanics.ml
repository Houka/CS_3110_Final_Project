open Terrain
open Feunit

let attack (map:terrain array) (u1: feunit) (u2: feunit) : feunit option =
  failwith "TODO"
let move (units: feunit array) (map: terrain array) (u1: feunit) (x,y) : feunit option =
  failwith "TODO"
let wait (u1: feunit) : feunit =
  failwith "TODO"
